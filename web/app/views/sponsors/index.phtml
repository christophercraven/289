


	{{ partial('layouts/member') }}
<div class="container">
        
    <p>Your Sponsors</p>
    <ul>
	{% for sponsor in sponsors %}
		<li>
            Name: {{ sponsor.name_spo|e }}
            <img src={{ sponsor.icon_app }} />
        </li>
	{% endfor %}
    </ul>
</div>